const { expect } = require('chai');
const wrap = require('./wrap');

describe('twoSum', () => {
  it('Returns empty array for empty array', () => {
    expect(twoSum([])).to.equal([])
  })
  // it('Does not use the same indices twice', () => {
  //   expect(twoSum([1, 2, 3, 4], 5).to.equal([]))
  // })



})
